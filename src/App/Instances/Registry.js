import * as TransactionTableInstance from './TransactionTableInstance';

const registry = [
  [
    TransactionTableInstance.TransactionTableInstance,
    TransactionTableInstance.mkProps,
  ]
];

export default registry;
